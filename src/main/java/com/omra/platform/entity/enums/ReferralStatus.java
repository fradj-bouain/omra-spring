package com.omra.platform.entity.enums;

/**
 * Referral status: PENDING (referred user just signed up), COMPLETED (conditions met for reward).
 */
public enum ReferralStatus {
    PENDING,
    COMPLETED
}
