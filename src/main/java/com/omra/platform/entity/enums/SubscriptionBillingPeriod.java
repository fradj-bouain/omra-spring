package com.omra.platform.entity.enums;

/**
 * Période de facturation affichée pour un forfait (le calendrier réel est sur {@code agency_subscriptions}).
 */
public enum SubscriptionBillingPeriod {
    MONTHLY,
    QUARTERLY,
    YEARLY,
    ONE_TIME
}
