package com.omra.platform.entity.enums;

/**
 * Type de parrain pour un voyageur nouvellement créé : un autre client (dossier existant) ou un agent de l'agence.
 */
public enum SponsorType {
    PILGRIM,
    AGENT
}
